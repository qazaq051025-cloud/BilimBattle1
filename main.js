/* =============================================
   BilimBattle — main.js
   App entry point
   ============================================= */

document.addEventListener('DOMContentLoaded', () => {
  // Restore session if exists
  restoreSession();
});
